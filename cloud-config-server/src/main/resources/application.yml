server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://gitlab.com/solomonfy/config-server/-/blob/main/application.yml
          clone-on-start: true